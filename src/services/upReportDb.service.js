import axios from "axios";
import store from "../store";

const handleUpReport = async (report) => {
  // const response = await axios.post('https://expressjs-postgres-production-08e2.up.railway.app/', {
  //   name: store.state.name[0],
  //   lastname: store.state.name[1],
  //   amount: store.state.mount,
  //   days: store.state.days,
  //   reinvestment: store.statereinvest,
  //   record: Date.now()
  // });
  // return response;
  return "We didn't pay the backend enough and he left everything half done. sorry :)";
}

export default handleUpReport;